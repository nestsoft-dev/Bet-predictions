import 'package:flutter/material.dart';

import '../constants/colors.dart';

class BetCodesPage extends StatelessWidget {
  const BetCodesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: myBlack,
    );
  }
}
